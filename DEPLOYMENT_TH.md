# 🚀 คู่มือ Deploy (ภาษาไทย)

ไฟล์นี้สรุปขั้นตอนสำหรับเตรียมระบบและนำ Omni Sales ขึ้น Vercel ในสภาพแวดล้อม Production

## 1. ตรวจสอบความพร้อมก่อน Deploy

- [ ] รัน `npm run build` ผ่านเรียบร้อย
- [ ] มีค่า Environment สำคัญครบ (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `STRIPE_SECRET_KEY`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`)
- [ ] ตั้งค่าบน Supabase แล้ว (ตาราง, RLS และผู้ใช้ Admin)
- [ ] ปรับข้อมูล Branding/Contact ในหน้า Landing Page ครบ
- [ ] เช็ก Security headers/middleware แล้ว

> ใช้คำสั่ง `npm run deploy:check` เพื่อเช็กค่า Environment อย่างรวดเร็ว

## 2. ขั้นตอน Deploy ขึ้น Vercel

1. ติดตั้ง Vercel CLI (ครั้งแรกเท่านั้น)
   ```bash
   npm i -g vercel
   ```
2. Login
   ```bash
   vercel login
   ```
3. (แนะนำ) ดึงค่า Environment ปัจจุบันจาก Vercel มาที่เครื่องเพื่อแก้ไข
   ```bash
   vercel pull --environment=production
   ```
4. เพิ่ม/อัปเดตค่า Environment ใน Vercel Dashboard หรือผ่านคำสั่ง
   ```bash
   vercel env add NEXT_PUBLIC_SUPABASE_URL production
   vercel env add NEXT_PUBLIC_SUPABASE_ANON_KEY production
   # เพิ่มตัวอื่น ๆ ตามความจำเป็น
   ```
5. Deploy ขึ้น Production
   ```bash
   vercel --prod
   ```
6. ตรวจสอบ log ผ่าน `vercel logs <deployment-url>` หรือจาก Dashboard

## 3. Script ช่วยตรวจสอบก่อน Deploy

- `npm run deploy:check` จะอ่านค่าจาก `.env.production` (ถ้ามี) หรือ `.env.local` และแจ้งว่ามีค่าไหนยังไม่ถูกตั้ง
- หากต้องการระบุไฟล์ env เอง ใช้ `npm run deploy:check -- path/to/.env.production`

> Script ไม่รัน `vercel --prod` ให้อัตโนมัติ เพื่อความปลอดภัย คุณต้องยืนยัน deploy เองทุกครั้ง

## 4. การแก้ไขปัญหาที่พบบ่อย

- **หน้าจอดำ / Auth ใช้งานไม่ได้**: ตรวจสอบว่ามีการตั้งค่า Supabase URL/Key หรือไม่ หากไม่มี ระบบจะขึ้นแถบเตือน “โหมดออฟไลน์” บน UI
- **Build ช้า/ค้าง**: ลองรัน `npm ci && npm run build` ใหม่ และตรวจสอบว่ามี WARN เกี่ยวกับ `next.config.ts` หรือไม่
- **Middleware Redirect วนลูป**: ตรวจสอบคุกกี้ `sb-access-token` และรายการ `publicRoutes` ใน `middleware.ts`

## 5. Checklist หลัง Deploy

- [ ] เข้าหน้า Landing Page ดูว่าไม่มีแถบเตือนสีเหลือง
- [ ] ลอง Login ด้วยบัญชีทดสอบ
- [ ] ทดสอบ workflow สำคัญ 2-3 flow (สร้างออเดอร์, ปรับสต็อก, ส่งรายงาน)
- [ ] เปิด Dashboard Performance (Sentry/Log) เพื่อดู error หลัง deploy

พร้อมแล้วก็ `vercel --prod` ได้เลยครับ ✨
